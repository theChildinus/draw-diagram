---
name: draw-diagram
description: "Create, edit, validate, and export editable Draw.io engineering diagrams on macOS using the user's local QA rules. Use only when explicitly invoked as $draw-diagram. Supports architecture, flowchart, sequence, ER, network, class, and state diagrams."
---

# Draw Diagram

独立完成日常工程图的新建、修改、导出和质量检查。保持 `.drawio` 为唯一可编辑事实源，不依赖其他绘图 Skill，也不在运行时联网获取说明。

## 固定边界

- 仅在用户明确调用 `$draw-diagram` 时使用；不要隐式接管普通绘图请求。
- 稳定支持架构图、流程图、时序图、ER 图、网络拓扑图、类图和状态图。
- 思维导图、甘特图、象限图、数据图表和 UI 线框图可以尝试，但不要承诺稳定工作流。
- 当前仅支持 macOS 和本机 Draw.io Desktop CLI。
- 始终保留 `.drawio`。SVG、PNG、PDF 仅在用户要求、目标文档需要或临时视觉检查时生成。
- 临时 Mermaid 文件和视觉检查导出物不是事实源，不要求用户维护。
- 创建或修改时只编辑独立候选 `.drawio`；最终目标通过 `scripts/deliver_drawio.py` 校验并交付，不直接用未验证内容覆盖。

## 按需读取

- 新建图、整体重绘或用户要求应用默认风格：读取 `references/style.json` 取得精确样式值。普通修改、校验和导出不读取。
- 创建、修改、布局或导出：完整读取 `references/execution.md`。
- 新建标准流程图、时序图、ER 图、类图或状态图并选择 Mermaid：完整读取 `references/mermaid.md`。
- 修改已有 `.drawio`、绘制架构图或网络拓扑图、需要精确控制容器、文本框、端口或连线，或 Mermaid 不能通过验收：完整读取 `references/xml.md`。
- 只有下列结构模式明确匹配时才读取 `references/cases.json`，并且最多采用其中一个案例。案例只提供结构启发，不能覆盖事实、用户要求、本 Skill 或默认样式。

| 当前图主要回答的问题 | 案例键 |
|---|---|
| 系统如何分层，主链如何跨层流转 | `layered-architecture` |
| 一个范围内部如何判断、执行并返回结果 | `internal-execution-flow` |
| 改造前后保留、增加、替换或下线了什么 | `before-after-comparison` |

## 绘图规则

### 内容和证据

- 一张图只回答一个主要问题。节点、关系、边界和阅读方向都服务于这个问题。
- 绘制现状图前，用当前代码、配置、接口或权威材料确认事实。
- 区分已确认事实、用户明确给定的设计、合理假设和证据缺口。
- 把职责归给实际执行或管理该功能的组件。区分运行进程、管理组件、工作目录和能力配置；不要把共享目录、Skill 集合或提示词画成运行时管理主体。
- 次要证据缺口放在交付说明中；缺口会改变节点、方向或关键链路时，只询问一个决定性问题。
- 不为画面整齐删除真实次要路径，也不为显得完整添加没有证据的关系。
- 信息量超过最终载体承受范围时拆成总览和细节图，不缩小字号硬塞。
- 同一概念存在多种叫法、受众可能不熟悉术语，或名称差异会改变语义时，才整理“来源名称、图中名称、含义”；简单图不额外维护术语表。

### 修改已有图和编辑性

- 默认延续原图有效的配色、字体、形状、间距和整体布局，只修改本次范围并保留无关内容与用户改动。
- 只有用户明确要求应用默认风格、要求重绘，或原布局已经妨碍表达时，才重做整体视觉体系。
- 修改前记录目标 SHA-256，将原文件复制为独立候选；不要在目标文件上直接试错。
- `.drawio` 是唯一事实源。Mermaid 只是临时输入，SVG、PNG、PDF 是导出物。
- 所有 `mxCell` ID 唯一；边包含 `source`、`target` 和相对 `mxGeometry`。
- XML 属性中的特殊字符必须转义，不在生成的 Draw.io XML 中写 XML 注释。
- 容器内子节点使用相对坐标；跨容器边放在公共层，避免被父容器裁切。
- 自动布局前保留可恢复文件；布局后重新校验文本框、端口、分组和连线。

### 布局和视觉语义

- 先确定从上到下或从左到右的主阅读方向，不混用多个主方向。
- 同层、同阶段或同类型对象保持对齐、尺寸和间距一致。
- 容器表示真实系统、团队、环境、集群、云账号或命名空间边界；子节点使用真正的父子关系，不只是叠放在大矩形上。
- 在当前图或当前页内突出核心主链，将支撑关系、状态回传和异常路径与主链区分；多页文件逐页判断。
- 新图默认采用白底、低饱和角色色、同色系深描边、圆角节点和简洁正交连线。精确值以 `references/style.json` 为准，图中没有对应角色时不使用该颜色。
- 填充色只表达稳定功能类别；形状表达资源类型；外框表达归属；线型表达关系；带文字的角标或边框表达状态。一个视觉维度不要同时承担多种含义。
- 运行状态使用带文字的绿、黄、红角标或边框；待建设和已废弃必须带文字。风险、故障和废弃优先使用红色边框或角标，不大面积铺红。
- 状态图或健康图使用“状态模式”：节点主体保持中性或弱化类别色，把绿、黄、红留给状态角标或边框。
- 颜色不能成为唯一载体。去掉颜色后，仍能通过形状、标签、边界、线型和角标读懂主要语义。
- 一张图通常不超过五至七个语义色。图例直接展示视觉样例及其功能，不只写颜色名称。
- 新图默认不使用阴影、渐变、纹理、手绘效果和无意义装饰。
- 大型背景容器宽、高都不小于 `300 px` 时，若使用 `rounded=1`，必须显式设置 `arcSize<=4`，默认 `arcSize=4`；也可以使用 `rounded=0`。普通卡片圆角可以使用 `10–14`，不能复用到大型容器。
- 多条关系汇聚并跨越多个区域时，优先使用真实存在的网关、代理、总线或注册中心，不虚构中转节点。
- 留白用于分组和阅读，不强行把卡片拉到等高，也不以大面积空白代替结构。

### 形状、字体和文字

- 默认使用矩形、圆角矩形、菱形、圆柱体、泳道和容器等通用形状。普通节点以 `160 × 60` 为视觉基线，再按内容和最终展示尺寸扩展。
- 决策使用菱形，数据存储使用圆柱体；同类对象保持同一形状语义。
- 只有用户明确指定厂商或领域视觉体系时才使用对应内置图标库。官方图标保留官方原色和比例，不重染、拉伸或旋转。
- 不能确认精确图标时，使用带清楚名称的通用形状，并在交付说明中说明回退。
- 图中文字依次保证：事实准确、沿用正式名称、受众能理解、表达简短、行文顺畅。
- 名称优先采用当前代码、配置、接口、正式文档或用户材料中的写法。旧图名称只作为线索，同一对象、动作和状态全图只使用一个名称。
- 函数名、接口、字段、命令和文件名保持原样。受众不熟悉的专业词或缩写在首次出现处、图例或图下写出常见中文含义。
- 新建纯英文图默认使用 `Helvetica`；包含中文时使用 `PingFang SC`。全图只使用一个字体族；修改已有图时保留其可用字体。
- 普通节点和关系标签默认 `12 pt`，区域标题或重点节点默认 `14 pt`。字体层级最多四级，同级字号和字重一致。
- 分区表头默认左对齐；普通模块、数据存储、外部系统和图例内容默认居中。批量修改前先区分对象类型。
- 图标题写清对象、视角和影响理解的现状、目标、计划或假设状态，尽量保持单行。
- 节点保留准确名称和当前图需要的一项主要职责；连线写清动作或传递内容；判断分支写可核对条件；异常路径写明失败、拒绝、超时或降级原因。
- 不用“数据流转”“能力输出”“异常处理”等泛化标签代替具体数据、动作和结果。
- `实时`、`自动`、`智能`、`统一`、`全量`、`高可用`、`安全`、`零侵入`、`无损`、`精准`等描述只有当前材料能证明时才使用；数量、时间、重试次数和范围保持来源原值。
- 删除宣传语、模糊归因、聊天套话、生造词和名词堆叠。不能确认的内容标为假设、待确认或移到交付说明。
- 文字拥挤时依次精简、换行、扩大图形、调整布局或拆图，不缩小字号勉强容纳。

### 独立文本框和点击边界

- 普通节点只有名称和一句说明时，写入图形自身的 `value`，不得用同级独立文本框覆盖空白节点。
- 只有需要独立定位、不同样式、复杂排版，或内置标签确实错位时才使用独立文本框；文本框必须是所属图形的子节点并设置 `connectable=0`。
- 文本框按渲染内容收紧宽高，不充当布局容器，不覆盖外框边线、端口或箭头。单行目标宽度为文字宽度加 `16–24 px`，无法测量时不超过外框宽度的 `80%`。
- UML 类成员行和 Draw.io 表格单元格等由 `stackLayout` 或 `tableLayout` 管理的结构化行，按布局容器规则验收。

外框几何为 `(x, y, width, height)` 时：

~~~text
safeX = max(12 px, min(24 px, width × 8%))
safeY = max(6 px, min(12 px, height × 12%))

text.x >= shape.x + safeX
text.x + text.width <= shape.x + shape.width - safeX
text.y >= shape.y + safeY
text.y + text.height <= shape.y + shape.height - safeY
~~~

子文本框以父图形 `(0, 0)` 为原点应用同一公式。放不下时使用内置标签、扩大外框或调整布局，不降低安全距离。只有静态检查不能确认且覆盖范围接近边界时，才在 Draw.io `100%` 缩放下分别点击外框四边，确认每次都能直接选中外框。

### 连线和箭头

- 边的 `source` 和 `target` 指向真实图形外框，不能指向标题、说明、边标签或独立文本框。
- 新图默认使用 `#334155`、`1.5 pt` 正交线、圆角折线和实心经典箭头。只有完全对齐、无需避障且直线更清楚时才使用直线。
- 先移动或对齐节点和端口，再添加折点。折点尽量少，优先从节点中点沿水平或垂直方向进出。
- 线宽和主链只在当前图或当前页内判断。先根据标题、目的和阅读方向列出边的端点、动作与方向；只有不经过就无法读完主要问题的核心链路使用 `2 pt`，其他关系默认 `1.5 pt`。
- 主链仍保留真实关系线型；异步主链可以是 `2 pt` 虚线，不能改成同步实线。

| 关系 | 颜色 | 线型 | 宽度 |
|---|---|---|---|
| 普通同步调用 | `#334155` | 实线 | `1.5 pt` |
| 主链路 | `#334155` | 保留关系线型 | `2 pt` |
| 异步消息、队列投递 | `#334155` | 虚线 `6 4` | `1.5 pt` |
| 状态回传、监控采集 | `#64748B` | 点线 `1 4` | `1.5 pt` |
| 次要依赖、辅助链路 | `#64748B` | 同步实线或异步虚线 | `1–1.5 pt` |
| 异常、失败、超时 | `#DC2626` | 保留关系线型并带文字 | `1.5 pt` |
| 强调的可观测性链路 | `#0E7490` | 点线 `1 4` | `1.5 pt` |

- 不根据起点或终点节点颜色给连线染色；白底上的必要关系线对比度至少为 `3:1`。
- 判断分支从菱形对应尖角沿分支方向直出，并标出条件。
- 箭头垂直进入目标外框，避开圆角、边角和圆柱弧线转折处。
- 正交箭头最后一段至少 `24 px`，末端 `24 px` 内没有折点；起点离开外框后至少保留 `16 px` 直线；相邻折点形成的中间段通常不短于 `16 px`。
- 标签放在最长且稳定的中间线段，不放在折点、交叉点、端口或箭头前最后 `32 px`。
- 连接线不穿过节点或文字；容器边框、图形轮廓和其他线不能遮住箭头及末段。
- 多条线进入同一节点时使用不同落点，相邻落点通常至少间隔 `12 px`。

## 工作流

### 1. 选择创作方式

用户明确指定的方式优先；否则按以下顺序选择：

- 修改已有 `.drawio`：复制为独立候选后编辑 XML。
- 新建架构图、网络拓扑图、复杂容器图、厂商图标图或精确排版图：使用 XML。
- 新建标准流程图、时序图、ER 图、类图或状态图：优先 Mermaid，再转换为 `.drawio`。
- Mermaid 无法表达或不能通过验收：进入 XML 局部精修，不要求用户确认创作工具变化。

先列清节点、关系、边界和主阅读路径，再决定位置与样式。不要为了布局改变事实或语义。Mermaid 转换成功后，后续修改都以 `.drawio` 为准。

### 2. 验证

从当前 Skill 目录运行静态验证：

~~~sh
python3 scripts/validate_drawio.py "/absolute/path/to/diagram.drawio"
~~~

修改了节点、端口或连线几何时，增加渲染后连线检查：

~~~sh
python3 scripts/validate_drawio.py "/absolute/path/to/diagram.drawio" --check-rendered-edges
~~~

该参数使用 Draw.io CLI 将每页临时导出为 SVG，并检查整页所有新旧连线。真实交叉和穿过非端点节点属于错误；短起段、中间段、末段及 `jumpStyle` 属于警告。临时 SVG 自动清理。

需要修正时使用 `--json`。每条诊断提供确定性的 `code`、`fingerprint`、精确 `subject`、测量 `evidence` 和允许的 `supported_fixes`。必须修正所有错误；警告逐项判断，不能确认无影响时也要修正。渲染后连线检查不能证明文字、标签和整体视觉合格。

### 3. 按视觉风险验收

- **无视觉变化**：文件移动或重命名、非视觉元数据调整，以及能确认不改变样式、文字和几何的结构清理。只运行静态校验，不导出预览。
- **局部视觉变化**：文字、字号、样式、单个节点、局部几何、端口或连线变化。需要时先运行渲染后连线检查；合并本轮修改后只导出一次，只查看受影响区域。
- **全局视觉变化**：新建图、全图布局、画布、字体或配色调整、多个区域联动，或局部预览无法判断整体效果。导出并检查整图。

预览最终显示尺寸下的字体、换行、裁切、遮挡、对齐、留白、连线交叉、箭头方向和标签位置。局部检查发现问题后只复查该区域；风险升级时再扩大范围。最终交付本身不自动触发全图预览。

只有独立文本框点击行为无法静态确认、复杂分组或端口存在疑点、导出与 XML 预期不一致，或用户明确要求时，才打开 Draw.io 做一次针对性检查。

### 4. 分级修正

1. 修正事实、节点、关系和标签。
2. 新建图最多尝试一次 Mermaid 重排或 XML 自动布局。
3. 用 XML 局部精修字体、文本框、容器、端口、连线和位置。
4. 局部补丁开始堆叠时，重建问题区域。

修改已有图时不自动重排整张图。自动布局完成不等于验收通过。

### 5. 交付

- 按 `references/execution.md` 记录目标修改前 SHA-256，并使用 `scripts/deliver_drawio.py` 交付独立候选；新文件使用预期状态 `missing`。
- 交付命令验证同一份候选字节，先完成全部导出，再替换目标；校验、导出或目标哈希检查失败时不得覆盖目标。
- 候选警告与目标基线比较：未变化的存量警告只记录；新增或测量结果变差的警告阻止交付。逐项检查后，用重复的 `--accept-warning <fingerprint>` 只接受对应警告。
- 必须声明 `--visual-risk none|local|global`。局部或全局变化只有在实际检查最终候选、使用 `--visual-review passed`，且 `--reviewed-candidate-sha256` 与候选一致时才能交付；脚本本身不执行视觉判断。
- 多页 `.drawio` 导出 SVG 或 PNG 时，使用 `--export-page PAGE=PATH` 明确页码；未指定页码的多页 PDF 默认导出全部页面。回执记录每个导出物对应的页。
- 保留紧凑 JSON 回执中的候选、目标和导出物哈希、校验摘要、视觉检查状态及 `committed`，不要把完整 XML、SVG 或 CLI 日志复制进交付说明。
- 交付 `.drawio` 绝对路径和用户要求或目标文档需要的导出物。说明事实核对范围、证据缺口、创作方式和实际完成的检查。
- 不把“XML 可解析”“CLI 成功”或“能打开”表述成视觉验收通过，也不因导出物嵌入 XML 而删除 `.drawio`。

## 验收清单

所有适用项目都必须满足：

### 事实和文字

- [ ] 节点、边界、关系、方向和标签与当前证据一致，设计、假设和证据缺口没有伪装成现状。
- [ ] 主问题、阅读入口和当前图或页的主链清楚。
- [ ] 名称来自当前材料或用户定义；同一对象、动作和状态使用同一名称，代码标识符保持原样。
- [ ] 标题、节点、连线、判断分支和状态标签写清所需对象、动作、条件、内容或结果，没有无依据能力描述、宣传语、模糊归因或生造词。

### 结构和视觉

- [ ] XML 可解析，ID 唯一，边端点存在，容器父子关系正确，独立文本框通过点击边界检查。
- [ ] 颜色、形状、边界、线型、角标和焦点分别承担稳定语义；去掉颜色后仍能读懂主要关系和状态。
- [ ] 字体、对齐、节点、容器和间距一致；大型背景容器没有沿用卡片高圆角。
- [ ] 最终尺寸下没有异常换行、裁切、遮挡、字体替换或过小文字。
- [ ] 不选择连线也能追踪起点、路径、方向和终点；箭头末段、标签、折点和交叉点符合约束。

### 校验和交付

- [ ] 校验器错误全部修正；新增或变差警告已修正或按 `fingerprint` 逐项接受。
- [ ] 按视觉风险完成对应预览，并把最终检查绑定到候选 SHA-256。
- [ ] `.drawio` 保持为事实源，导出物来自同一候选快照且页范围明确。
- [ ] 只生成用户要求、目标文档或临时检查需要的导出物；只有必要时才打开 Draw.io 图形界面。

## 禁止事项

- 不读取或依赖其他绘图 Skill。
- 不在运行时获取远程 Mermaid、XML 或样式参考。
- 不自动操作 Draw.io 图形界面完成日常验收。
- 不默认维护 `.drawio + SVG` 两份事实源。
- 不为表面对称填充无效节点、拉伸容器或压缩文字。
- 不覆盖、清理或重排任务范围外的用户文件。
