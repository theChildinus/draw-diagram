---
name: draw-diagram
description: "Create, edit, review, validate, and export editable Draw.io engineering diagrams with the user personal rules. Use only when the user explicitly invokes $draw-diagram. Stable scope: architecture, flowchart, sequence, ER, network topology, class, and state diagrams on macOS."
---

# Draw Diagram

独立完成日常工程图的新建、修改、导出和质量检查。新建图默认采用白底、低饱和角色色、圆角节点和简洁正交连线的视觉风格。保持 `.drawio` 为唯一可编辑事实源，不依赖其他绘图 Skill，也不在运行时联网获取说明。

## 固定边界

- 仅在用户明确调用 `$draw-diagram` 时使用；不要隐式接管普通绘图请求。
- 稳定支持架构图、流程图、时序图、ER 图、网络拓扑图、类图和状态图。
- 思维导图、甘特图、象限图、数据图表和 UI 线框图可以尝试，但不要承诺稳定工作流。
- 第一版只承诺 macOS 和本机 Draw.io Desktop CLI。
- 始终保留 `.drawio`。SVG、PNG、PDF 仅在用户要求、目标文档需要或临时视觉检查时生成。
- 临时 Mermaid 文件和视觉检查导出物不是事实源，不要求用户维护。

## 参考文件路由

1. 每次任务开始都完整读取 `references/drawio-guidelines.md`，并读取 `references/default-visual-style.json` 取得精确样式值。
2. 创建、修改、布局或导出时完整读取 `references/drawio-execution.md`。
3. 新建标准流程图、时序图、ER 图、类图或状态图并选择 Mermaid 时，完整读取 `references/mermaid-reference.md`。
4. 修改已有 `.drawio`、绘制架构图或网络拓扑图、需要精确控制容器/文本框/端口/连线，或 Mermaid 不能通过验收时，完整读取 `references/drawio-xml-reference.md`。
5. 案例只按下表最多读取一个。先说明匹配理由；没有明确匹配时不读案例。

| 当前图主要回答的问题 | 可选案例 |
|---|---|
| 系统如何分层，主链如何跨层流转 | `references/case-layered-architecture.json` |
| 一个范围内部如何判断、执行并返回结果 | `references/case-internal-execution-flow.json` |
| 改造前后保留、增加、替换或下线了什么 | `references/case-before-after-comparison.json` |

案例只提供结构模式，不能覆盖事实、用户要求、字体、文本框边界、连线、编辑性或验收规则，也不能复制历史项目内容和视觉样式。

## 工作流

### 1. 明确任务和事实

- 确认或合理推断目的、受众、载体、核心问题、关系类型、文件路径和交付格式。
- 绘制现状图前，先检查当前可用的代码、配置、接口或权威材料。
- 图中文字按以下顺序处理：事实准确、沿用正式名称、受众能理解、表达简短、行文顺畅；后面的要求不能损害前面的要求。
- 只有同一概念存在多种叫法、受众可能不熟悉术语，或名称差异会改变语义时，才列出“来源名称、图中名称、含义”；简单图不额外维护术语表。
- 次要证据缺口不阻塞绘制，在交付说明中列出。
- 缺口会改变节点、方向或关键链路时，暂停并只问一个决定性问题。
- 不把未确认内容悄悄画成事实，也不默认把“待确认”塞进图中。

### 2. 选择创作方式

按以下顺序选择，用户明确指定的方式优先：

- 修改已有 `.drawio`：直接编辑 XML。
- 新建架构图、网络拓扑图、复杂容器图、厂商图标图或精确排版图：使用 XML。
- 新建标准流程图、时序图、ER 图、类图或状态图：优先 Mermaid，再转换为 `.drawio`。
- Mermaid 无法表达或不能通过硬性验收：进入 XML 局部精修，不要求用户确认创作工具变化。

先列清节点、关系、边界和主阅读路径，再决定位置与样式。不要为了布局好看改变事实或语义。

- 每张图单独确定核心阅读链；多页 `.drawio` 文件按页分别判断。`主链路`只指当前图/页为回答主要问题而选定的核心链路，不是跨图、跨服务类型的固定清单。
- 线宽只在当前图/页内比较：普通或辅助关系通常为 `1.5 pt`，当前图/页的核心主链为 `2 pt`。异步、状态回传和异常关系保留各自的线型与颜色；只有同时属于当前核心链路时，才继承 `2 pt`。

### 3. 创建或修改

- 修改已有图时，保留其有效的配色、字体、形状风格和整体布局，只处理本次范围。只有用户明确要求改成默认风格、要求重绘，或原布局已经妨碍表达时，才整体换肤或改版。
- 新图默认使用 `references/default-visual-style.json`。用户给出其他视觉要求时，以用户要求为准。
- 使用白色画布、低饱和浅色填充、对应的较深描边、统一圆角和深灰色细连线。填充色表示功能类别，形状表示元素类型，边界表示归属，线型表示关系，带文字的角标或边框表示状态；颜色不能成为唯一的信息载体。默认不加阴影、渐变、纹理、手绘效果或装饰性图标。
- 默认使用 Draw.io 通用形状。只有用户明确指定 AWS、Azure、Kubernetes 等体系时才使用对应内置图标；无法确认准确图标时回退到带清楚文字的通用形状并说明。
- 官方图标保留官方原色和比例，不重染、不拉伸、不旋转；自定义语义放在容器、边框、分区、标签和连线上。
- Mermaid 只作为临时创作输入。转换成功后，后续修改都以 `.drawio` 为准。
- 修改已有图时，先按当前图/页列出边的端点、动作、方向和关系类别，再标记核心主链、辅助链、状态回传和异常路径；不能因为另一张图使用过某个组件或关系就直接套用同一线宽。
- 标题直接写清对象和视角。现状、目标、计划或假设会影响理解时，在标题、角标或图例中明确标出，不能混写。
- 排版前单独检查图中文字。节点名称使用材料中已有的产品、组件、资源或业务对象名称；节点说明写清具体动作和对象；连线标签写清调用、传递、返回或状态变化。不要用生造的缩写、四字概括或抽象口号替代准确表达。
- 函数名、接口、字段、命令和文件名保持原样。受众可能不熟悉的缩写或专业词，在首次出现处、图例或图下说明中用常见中文简要解释；不要为了省空间另造简称。
- `实时`、`自动`、`智能`、`统一`、`全量`、`高可用`、`安全`、`零侵入`、`无损`、`精准`等能力、范围或质量描述，只有当前材料能够证明时才使用；否则改写为实际机制或删除。数量、时间、重试次数和适用范围保持来源原值，不自行推断。
- 文字需要精简时，删除次要信息或拆图。不能把明确含义压成“能力支撑”“智能协同”“数据流转”等无法说明具体动作和对象的标签，也不能靠缩小字号容纳长句。

### 4. 验证

从当前 Skill 目录运行静态验证：

~~~sh
python3 scripts/validate_drawio.py "/absolute/path/to/diagram.drawio"
~~~

修改了节点、端口或连线几何时，增加渲染后连线检查：

~~~sh
python3 scripts/validate_drawio.py "/absolute/path/to/diagram.drawio" --check-rendered-edges
~~~

该参数使用 Draw.io CLI 将每页临时导出为 SVG，并检查整页所有新旧连线，不只检查本轮修改的边。真实交叉和穿过非端点节点属于错误；短起段、中间段、末段及 `jumpStyle` 属于需要逐项判断的警告。临时 SVG 自动清理，不成为第二份事实源。

必须修正所有错误。警告需要逐项判断；不能确认无影响时也要修正。渲染后连线检查仍不能证明文字、标签和整体视觉合格。

### 5. 按视觉风险分级验收

先判断本次变化是否会改变最终渲染，再选择最小验收范围：

- **无视觉变化**：文件移动或重命名、非视觉元数据调整，以及能确认不改变样式、文字和几何的结构清理。只运行静态校验；不导出预览，也不调用图片查看能力。
- **局部视觉变化**：修改文字、字号、样式、单个节点、局部几何、端口或连线。涉及节点、端口或连线几何时先运行渲染后连线检查；合并本轮修改后只导出一次，并只查看受影响区域；不要在每个补丁后查看整图。
- **全局视觉变化**：新建图、全图布局、画布、字体或配色调整、多个区域联动，或局部预览无法判断整体效果。导出并检查整图。

需要视觉预览时，检查最终显示尺寸下的字体、换行、裁切、遮挡、对齐、留白、连线交叉、箭头方向和标签位置。局部预览发现问题后，修正并仅复查该区域；风险升级时再扩大范围。最终交付本身不自动触发全图预览。

同时按最终受众的阅读方式通读图中文字：标题能说明对象、范围和必要的状态，术语前后一致，节点和连线写清与当前问题有关的动作、对象、传递内容或结果。逐项核对能力、范围、质量、数量和时间描述。删除宣传语、套话、模糊归因、无信息量的形容词和聊天式说明。自然表达不等于随意口语，也不要求给工程图加入情绪或个性。

只有独立文本框点击行为无法静态确认、复杂分组或端口存在疑点、导出结果与 XML 预期不一致，或用户明确要求时，才打开 Draw.io 做一次针对性检查。

### 6. 分级修正

按顺序处理，不要直接整图重写：

1. 修正事实、节点、关系和标签。
2. 新建图最多尝试一次 Mermaid 重排或 XML 自动布局。
3. 用 XML 局部精修字体、文本框、容器、端口、连线和位置。
4. 局部补丁开始堆叠时，重建问题区域。

修改已有图时不自动重排整张图。自动布局完成不等于验收通过。

### 7. 交付

- 交付 `.drawio` 的绝对路径，以及用户要求或目标文档需要的导出物。
- 不因生成了嵌入 XML 的导出物而删除 `.drawio`。
- 说明事实核对范围、未解决的证据缺口、使用的创作方式和实际完成的检查。
- 不把“XML 可解析”“CLI 成功”或“能打开”表述成视觉验收通过。

## 硬性验收门槛

- 事实、节点、关系、方向和边界与当前证据一致。
- XML 可解析，ID 唯一，所有边的 `source` 和 `target` 存在。
- 外层分区和大型背景容器不得沿用普通卡片的高圆角；符合 `drawio-guidelines.md` 定义的大型背景容器若启用圆角，必须显式设置 `arcSize<=4`，默认使用 `4`。
- 全图使用同一字体族；字体层级不超过四级；同级字号和字重一致。
- 新图的功能类别、状态角标、关系标签、填充色、描边色和连线样式符合 `references/default-visual-style.json`；角色不适用时使用服务蓝色或中性节点，不为装饰强行增加颜色。
- 填充色、形状、边界、线型、状态和焦点分别承担一种信息维度；同一颜色和线型在全图保持同一含义。
- 图中没有颜色时仍能通过形状、标签、边界、线型和角标读懂主要语义；使用超过一个类别色、状态色或关系线型时提供简短图例。
- 连线宽度必须按当前图/页的语义判定：`2 pt` 只用于该图/页明确承担核心阅读路径的边，不能作为跨图的全局组件或关系默认值。
- 状态图或运行健康图使用状态模式：绿、黄、红只用于带文字的状态角标或边框，消息节点不得使用会被误认为警告的橙黄色填充。
- 文字放不下时先精简、扩大图形或调整布局，不靠缩小字号硬塞。
- 标题说明对象、范围和必要的状态；现状、目标、计划、假设和待确认内容没有混写。
- 图中名称和表述有当前材料或用户定义作为依据；不生造术语、缩写、组件职责或概括性口号。
- 同一个对象、动作和状态全图使用同一个名称；函数名、接口、字段、命令和文件名保持原样，不为避免重复而替换同义词。
- 节点说明和连线标签写明具体动作与对象。受众可能不熟悉的专业词或缩写已在首次出现处、图例或图下说明中解释，精简不能损害准确性。
- 能力、范围、质量、数量和时间描述有当前材料依据；没有把“实时”“自动”“高可用”等无法核对的判断写成事实。
- 图中文字没有宣传化表达、空泛判断、模糊归因、聊天套话或为了形式强凑的排比。
- 普通节点只有名称和一句说明时，必须写入图形自身的 `value`；不得用同级独立文本框覆盖空白节点。确需独立排版时，文本框必须是所属图形的子节点。
- 独立文本框按内容收紧，设置 `connectable=0`，并满足 `safeX`、`safeY` 安全边界。
- 连线只连接真实图形，不连接标题、说明或独立文本框。
- 箭头可见，方向正确，不贴着圆角落点，不用短钩或多余折点掩盖问题。
- 修改节点、端口或连线几何后，渲染检查没有未解决的边线交叉或穿节点错误；短段和 `jumpStyle` 警告已逐项判断。
- 需要视觉预览时，导出结果没有异常换行、裁切、遮挡、字体替换或无法追踪的关系。
- `.drawio` 保持可编辑，且与导出内容一致。

## 禁止事项

- 不读取其他绘图 Skill，尤其是已卸载的原生 `drawio` Skill。
- 不在运行时获取远程 Mermaid、XML 或样式参考。
- 不自动操作 Draw.io 图形界面完成日常验收。
- 不默认维护 `.drawio + SVG` 两份事实源。
- 不为表面对称填充无效节点、拉伸容器或压缩文字。
- 不覆盖、清理或重排任务范围外的用户文件。
